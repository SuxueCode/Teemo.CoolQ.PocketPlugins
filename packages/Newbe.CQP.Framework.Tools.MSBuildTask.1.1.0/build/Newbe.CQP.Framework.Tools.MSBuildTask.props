<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="4.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <!-- try to import project-specific settings; also use fixed name that was injected into project by NuGet -->
  <PropertyGroup>
    <!-- at this point only MSBuild* property are available -->
    <MSBuildNewbeCQPFramework>$(MSBuildProjectDirectory)\Newbe.CQP.Framework.props</MSBuildNewbeCQPFramework>
  </PropertyGroup>
  <Import Project="$(MSBuildNewbeCQPFramework)" Condition="Exists($(MSBuildNewbeCQPFramework))"/>
</Project>